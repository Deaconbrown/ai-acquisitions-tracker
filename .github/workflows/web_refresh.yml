name: Web Refresh

on:
  schedule:
    - cron: '30 */2 * * *'
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client beautifulsoup4 lxml

      - name: Write Drive token
        run: |
          mkdir -p /tmp/credentials
          echo "${{ secrets.PERSONAL_DRIVE_TOKEN_B64 }}" | base64 -d > /tmp/credentials/personal_drive_token.pickle

      - name: Run web refresh
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          PERSONAL_DRIVE_TOKEN_B64: ${{ secrets.PERSONAL_DRIVE_TOKEN_B64 }}
          SOURCES_JSON_B64: ${{ secrets.SOURCES_JSON_B64 }}
        run: python "[01] Scripts/newsletter_builder.py" --web-only

      - name: Commit and push updated website
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add newsletter/latest.html public/
          git diff --cached --quiet || git commit -m "Auto-refresh: website update $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push origin master